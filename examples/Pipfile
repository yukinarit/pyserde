[[source]]
url = "https://pypi.python.org/simple"
verify_ssl = true
name = "pypi"

[requires]
python_version = "3.6"

[packages]
pyserde = { path = "../", editable = true }
toml = { version = "*" }
pytoml = { version = "*" }
requests = { version = "*" }
